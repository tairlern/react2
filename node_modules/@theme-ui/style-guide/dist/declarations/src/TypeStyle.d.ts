import { ComponentProps } from 'react';
import Card from './Card';
export interface TypeStyleProps extends ComponentProps<typeof Card> {
    fontSize?: number | string;
    fontFamily?: string;
    lineHeight?: string;
    fontWeight?: string;
    truncate?: boolean;
}
export declare const TypeStyle: ({ fontSize, fontFamily, lineHeight, fontWeight, children, truncate, ...props }: TypeStyleProps) => import("react").JSX.Element;
export default TypeStyle;
