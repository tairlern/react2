import { ComponentProps } from 'react';
export interface CardProps extends ComponentProps<'div'> {
}
export declare const Card: (props: CardProps) => import("react").JSX.Element;
export default Card;
